#include <iostream>
using namespace std;

class rational {

	public:
		// Constructors & Destructor
		// Accessor & Mutator methods
		// Greatest Common Divisor method (Euclidean algorithm)
		// Simplify method (reducing rational to simplest form)
		// Overloaded operators (+ - * / < > == !=) 


	private:
		int n, d; // Numerator & Denominator
}